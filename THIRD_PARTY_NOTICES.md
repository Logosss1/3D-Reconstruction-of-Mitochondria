# Third-Party Notices

The project depends on open-source scientific Python packages listed in
[`environment.yml`](environment.yml), including PyTorch, NumPy, SciPy,
scikit-image, Zarr, numcodecs, trimesh, tqdm, Matplotlib, TensorBoard,
plyfile, fibsem-tools, and tifffile.

Each dependency remains under its own license. This repository's MIT license
applies only to original project code unless a file states otherwise. Users
should consult the package distributions and upstream repositories for the
complete license texts and notices.

The project also references OpenOrganelle/Janelia datasets. Dataset attribution
and reuse are governed by the source records described in
[`DATA_SOURCES.md`](DATA_SOURCES.md), not by this repository's code license.
